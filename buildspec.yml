version: 0.1
phases:
  install:
    commands:
      - aws cloudformation package --template-file CodePipeLineCloudFormationYaml.yaml --s3-bucket codepipeline-us-east-1-585635839497 --output-template-file outputsamtemplate.yml
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - outputsamtemplate.yml
